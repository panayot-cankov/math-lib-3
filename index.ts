// Adds i to j.
export function add(i: number, j: number) {
    return i + j;
}

export function cos(a: number) {
    return Math.cos(a);
}

export function sin(a: number) {
    // Fix
    return Math.sin(a);
}
